# Secrets — NEVER commit
.env
.env.*
!.env.example
*.key
*.pem
credentials.json
secrets/

# Python
__pycache__/
*.pyc
*.pyo
*.egg-info/
.venv/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Build
dist/
build/

# Logs
*.log
logs/

# Runtime state
session_mapping.json
channel_sessions.json
usage_pins.json
outbox.json
tmux_sessions/
data/
.codex/.sync/

# User config
topic_config.json

# Local task specs (per-instance, not part of the distro)
.tasks/

# Temp files
tmp/
*.tmp
data/.tmp/

# IDE
.vscode/
.idea/
*.swp

# OS
.DS_Store

# MCP runtime configs (may contain credentials)
.mcp.json
.mcp.bot.json

# memory-mcp filelock
CLAUDE.md.lock
*.md.lock

# timestamped backup files (topic_config.json.bak-*, transcriber.py.bak-*, …)
# — never commit these to the repo. (audit 2026-07-02)
*.bak-*

# local MCP task-runner config (contains API keys)
.mcp.tasks.json

# Private working docs live in the owner vault, never in this repo
.docs/
